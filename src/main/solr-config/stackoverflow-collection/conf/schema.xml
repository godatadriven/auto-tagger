<?xml version="1.0" encoding="utf-8" ?>
<schema name="stackoverflow-questions" version="1.4">
    <uniqueKey>question_id</uniqueKey>
    <defaultSearchField>title</defaultSearchField>
    <types>
        <fieldType name="string" class="solr.StrField" />

        <!-- From the standard solr example: -->

        <!-- A general text field that has reasonable, generic
         cross-language defaults: it tokenizes with StandardTokenizer,
	 removes stop words from case-insensitive "stopwords.txt"
	 (empty by default), and down cases.  At query time only, it
	 also applies synonyms. -->
        <fieldType name="text_general" class="solr.TextField" positionIncrementGap="100">
            <analyzer type="index">
                <tokenizer class="solr.StandardTokenizerFactory" />
                <!--<filter class="solr.StopFilterFactory" ignoreCase="true" words="stopwords.txt" />-->
                <!-- in this example, we will only use synonyms at query time
                <filter class="solr.SynonymFilterFactory" synonyms="index_synonyms.txt" ignoreCase="true" expand="false"/>
                -->
                <filter class="solr.LowerCaseFilterFactory" />
            </analyzer>
            <analyzer type="query">
                <tokenizer class="solr.StandardTokenizerFactory" />
                <!--<filter class="solr.StopFilterFactory" ignoreCase="true" words="stopwords.txt" />-->
                <filter class="solr.SynonymFilterFactory" expand="true" ignoreCase="true" synonyms="synonyms.txt" />
                <filter class="solr.LowerCaseFilterFactory" />
            </analyzer>
        </fieldType>

        <fieldType name="boolean" class="solr.BoolField" sortMissingLast="true" />
        <fieldType name="long" class="solr.TrieLongField" positionIncrementGap="0" precisionStep="0" />
        <fieldType name="int" class="solr.TrieIntField" positionIncrementGap="0" precisionStep="0" />

    </types>
    <fields>
        <field name="question_id" indexed="true" required="true" stored="true" type="long" />
        <field name="body" indexed="true" stored="true" type="text_general" />
        <field name="is_answered" indexed="true" stored="true" type="boolean" />
        <field name="link" indexed="true" stored="true" type="string" />
        <field name="score" indexed="true" stored="true" type="int" />
        <field name="tags" indexed="true" multiValued="true" stored="true" type="string" />
        <field name="title" indexed="true" stored="true" type="text_general" />
        <field name="creation_date" indexed="false" stored="false" type="string" />
    </fields>
</schema>